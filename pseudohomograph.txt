promt for first filename
promt for second filename

logic to compare?

run canonicalization function(This is important)

./cs470/../cs470/filename.txt
/home/cs470/filename.txt

http://www.cplusplus.com/reference/cstdlib/
getenv 

/* getenv example: getting path */
#include <stdio.h>      /* printf */
#include <stdlib.h>     /* getenv */

int main ()
{
  char* pPath;
  pPath = getenv ("PATH");
  if (pPath!=NULL)
    printf ("The current path is: %s",pPath);
  return 0;
}

std::cout << "Current path is " << fs::current_path() << '\n';


main
get working directory
display menu (?)
prompt user
  filename1
  filename2
to lower case everything (maybe)

canonize filename2
compare both filenames
  display results